\documentclass[border=1]{standalone}
\usepackage{calculator}
\usepackage{tikz}

\tikzset{
  point/.style={ draw, scale=0.25, circle, fill },
  letter/.style={ scale=0.55, below }
}

\begin{document}
\def\width{6}
\def\delta{0.15}
\def\epsilon{0.6}
\def\maxdist{0.8}

\begin{tikzpicture}
  \draw (0, 0) rectangle (\width, \width);

  \draw[dashed, blue, fill=black!10] (\maxdist, \maxdist) rectangle (\width - \maxdist, \width - \maxdist);

  \draw[stealth-stealth] (\width - \maxdist, 1.5) -- (\width, 1.5);
  \node[letter] at (\width - \maxdist/2.0, 1.5) {$maxdist$};

  \node[point] at (1.50, 4.00) {};
  \node[point] at (2.00, 3.00) {};
  \node[point] at (2.00, 3.50) {};

  \def\x{5.6}
  \def\y{3.75}
  \draw[] (\x, \y) circle (\epsilon / 2);
  \coordinate (A1) at (\x, \y+\delta);\node[point] at (A1) {};
  \coordinate (B1) at (\x, \y)       ;\node[point] at (B1) {};
  \coordinate (C1) at (\x, \y-\delta);\node[point] at (C1) {};
  \def\xa{\x - \maxdist}
  \draw[] (\xa, \y) circle (\epsilon / 2);
  \coordinate (A2) at (\xa, \y+\delta);\node[point] at (A2) {};\draw[-latex] (A1) -- (A2);
  \coordinate (B2) at (\xa, \y);       \node[point] at (B2) {};\draw[-latex] (B1) -- (B2);
  \coordinate (C2) at (\xa, \y-\delta);\node[point] at (C2) {};\draw[-latex] (C1) -- (C2);
  \def\xb{\xa - \maxdist}
  \def\ya{\y + \maxdist * 0.66}
  \draw[] (\xb, \ya) circle (\epsilon / 2);
  \coordinate (A3) at (\xb, \ya+\delta);\node[point] at (A3) {};\draw[-latex] (A2) -- (A3);
  \coordinate (B3) at (\xb, \ya);       \node[point] at (B3) {};\draw[-latex] (B2) -- (B3);
  \coordinate (C3) at (\xb, \ya-\delta);\node[point] at (C3) {};\draw[-latex] (C2) -- (C3);

  \def\x{5.6}
  \def\y{2.25}
  \draw[] (\x, \y) circle (\epsilon / 2);
  \coordinate (D1) at (\x, \y+\delta);\node[point] at (D1) {};
  \coordinate (E1) at (\x, \y)       ;\node[point] at (E1) {};
  \coordinate (F1) at (\x, \y-\delta);\node[point] at (F1) {};
  \def\xa{\x - \maxdist}
  \draw[] (\xa, \y) circle (\epsilon / 2);
  \coordinate (D2) at (\xa, \y+\delta);\node[point] at (D2) {};\draw[latex-] (D1) -- (D2);
  \coordinate (E2) at (\xa, \y);       \node[point] at (E2) {};\draw[latex-] (E1) -- (E2);
  \coordinate (F2) at (\xa, \y-\delta);\node[point] at (F2) {};\draw[latex-] (F1) -- (F2);
  \def\xb{\xa - \maxdist}
  \def\ya{\y - \maxdist * 0.66}
  \draw[] (\xb, \ya) circle (\epsilon / 2);
  \coordinate (D3) at (\xb, \ya+\delta);\node[point] at (D3) {};\draw[latex-] (D2) -- (D3);
  \coordinate (E3) at (\xb, \ya);       \node[point] at (E3) {};\draw[latex-] (E2) -- (E3);
  \coordinate (F3) at (\xb, \ya-\delta);\node[point] at (F3) {};\draw[latex-] (F2) -- (F3);

  \def\x{2.75}
  \def\y{0.4}
  \draw[] (\x, \y) circle (\epsilon / 2);
  \coordinate (G1) at (\x, \y+\delta);\node[point] at (G1) {};
  \coordinate (H1) at (\x, \y)       ;\node[point] at (H1) {};
  \coordinate (I1) at (\x, \y-\delta);\node[point] at (I1) {};
  \def\xa{\x - \maxdist * 1}
  \def\ya{\y + \maxdist * 1}
  \draw[] (\xa, \ya) circle (\epsilon / 2);
  \coordinate (G2) at (\xa, \ya+\delta);\node[point] at (G2) {};\draw[-latex] (G1) -- (G2);
  \coordinate (H2) at (\xa, \ya);       \node[point] at (H2) {};\draw[-latex] (H1) -- (H2);
  \coordinate (I2) at (\xa, \ya-\delta);\node[point] at (I2) {};\draw[-latex] (I1) -- (I2);
\end{tikzpicture}

\end{document}
